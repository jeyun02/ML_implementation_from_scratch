
from re import X


class LinearRegression:
   def __init__(self, X,y):
      self.X_train = X
      self.y_train = y
   
   def fit(self,X, y):
      ## Optimize the function
      
      return 
   
   
   def predict(self, X):
      # fit 에서 구한 함수에 그대로 대입한다.




      return